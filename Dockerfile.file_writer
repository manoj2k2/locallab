# Dockerfile for the File Writer Output Adapter
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV PYTHONPATH=/

CMD ["python", "-u", "-m", "src.output_adapters.file_writer"]
